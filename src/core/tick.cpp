// core/tick.cpp — RED-GATE STUB. Returns immediately without simulating.
// Replaced with the real implementation once the .test scenarios are
// confirmed to fail against this stub.
#include "tick.hpp"

namespace baba::core {

TickReport apply_tick(World& /*world*/, Input /*input*/) {
    return TickReport{};
}

}  // namespace baba::core
